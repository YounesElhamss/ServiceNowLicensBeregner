{
  "name": "ServiceowLicenseCalculator",
  "version": "0.0.1",
  "description": "System that can calculate created elements",
  "keywords": "",
  "license": "",
  "author": "",
  "engines": {
    "npm": "8.7.0"
  },
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
